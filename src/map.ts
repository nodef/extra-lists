import type {Lists, mapFn} from './_types';

/**
 * Updates values based on map function.
 * @param x lists
 * @param fn map function (v, k, x)
s */
function* map<T, U, V>(x: Lists<T, U>, fn: mapFn<T, U, V>) {
  var i = -1;
}
export default map;
